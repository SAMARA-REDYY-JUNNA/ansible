- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: latest
  - name: start nginx
    ansible.builtin.command:
      name: nginx
      state: started
      enable: yes

